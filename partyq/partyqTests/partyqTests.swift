@testable import partyq
import Quick
import Nimble

class partyqTest: QuickSpec {
    override func spec() {
        describe("example test") {
            it("passes") {
                expect(1+1).to(equal(2))
            }
        }
    }
}
