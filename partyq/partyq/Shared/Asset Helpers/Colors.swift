import SwiftUI

enum Colors {
    static let primary100 = Color(R.color.primary100()!)
    static let primary200 = Color(R.color.primary200()!)
    static let primary300 = Color(R.color.primary300()!)
    static let primary400 = Color(R.color.primary400()!)
    static let primary500 = Color(R.color.primary500()!)
    static let primary600 = Color(R.color.primary600()!)
    static let secondaryColor = Color(R.color.secondaryColor()!)
    static let secondaryLightColor = Color(R.color.secondaryLightColor()!)
    static let secondaryDarkColor = Color(R.color.secondaryDarkColor()!)
}
