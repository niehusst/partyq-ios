import SwiftUI

enum Colors {
    static let primary100 = Color(R.color.primary100()!)
    static let primary200 = Color(R.color.primary200()!)
    static let primary300 = Color(R.color.primary300()!)
    static let primary400 = Color(R.color.primary400()!)
    static let primary500 = Color(R.color.primary500()!)
    static let primary600 = Color(R.color.primary600()!)

    static let gray000 = Color(R.color.gray000()!)
    static let gray100 = Color(R.color.gray100()!)
    static let gray200 = Color(R.color.gray200()!)
    static let gray300 = Color(R.color.gray300()!)
    static let gray400 = Color(R.color.gray400()!)
    static let gray500 = Color(R.color.gray500()!)
    static let gray600 = Color(R.color.gray600()!)
    static let gray700 = Color(R.color.gray700()!)
    static let gray800 = Color(R.color.gray800()!)
    static let gray900 = Color(R.color.gray900()!)
    static let gray999 = Color(R.color.gray999()!)

    static let secondaryColor = Color(R.color.secondaryColor()!)
    static let secondaryLightColor = Color(R.color.secondaryLightColor()!)
    static let secondaryDarkColor = Color(R.color.secondaryDarkColor()!)

    static let backgroundColor = primary100
}
